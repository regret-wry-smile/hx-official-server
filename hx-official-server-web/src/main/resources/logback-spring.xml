<?xml version="1.0" encoding="UTF-8"?>
<configuration  scan="true" scanPeriod="60 seconds" debug="false">
    <contextName>logback</contextName>
    <include resource="org/springframework/boot/logging/logback/base.xml" />
    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--<appender name="logstash" class="net.logstash.logback.appender.LogstashTcpSocketAppender">-->
        <!--<destination>47.111.169.108:8884</destination>-->
        <!--&lt;!&ndash; encoder必须配置,有多种可选 &ndash;&gt;-->
        <!--<encoder charset="UTF-8" class="net.logstash.logback.encoder.LogstashEncoder">-->
            <!--&lt;!&ndash;在生成的json中会加这个字段&ndash;&gt;-->
            <!--<springProfile name="dev">-->
                <!--<customFields>{"appname":"dev-teacher-platform-face"}</customFields>-->
            <!--</springProfile>-->
            <!--<springProfile name="pro,test,zhuoyue">-->
                <!--<customFields>{"appname":"teacher-platform-face"}</customFields>-->
            <!--</springProfile>-->
        <!--</encoder>-->
    <!--</appender>-->

    <!--按天生成日志-->
    <appender name="logFile"  class="ch.qos.logback.core.rolling.RollingFileAppender">
        <Prudent>true</Prudent>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>
                ../logs/teacher-platform-face/%d{yyyy-MM-dd}/%d{yyyy-MM-dd}[%i].log
            </FileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} -%msg%n
            </Pattern>
        </layout>
    </appender>

    <root level="error">
        <appender-ref ref="console"/>
        <appender-ref ref="logFile" />
    </root>

</configuration>
